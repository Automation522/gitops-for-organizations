cluster:
  name: small1
  domain: skyr.dca.scc
  fqdn: small1.skyr.dca.scc
  address: "https://kubernetes.default.svc"
  environment: dev
  datacenter: europe
  cloud: vmware
  region: eu
  registry: quay-dev.io

catalogSource:
  rh_catalog_version: v4.10

ingress:
  controller:
    syslog:
      address: 10.9.0.1
